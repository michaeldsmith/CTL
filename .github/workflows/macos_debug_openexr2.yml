name: macOS-OpenEXR2-Debug

on:
  push:
    paths-ignore:
      - 'README.md'
      - 'doc/**'
  pull_request:
    paths-ignore:
      - 'README.md'
      - 'doc/**'

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Debug

jobs:

  github-actions-environment-variables-macos:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, macos-12, macos-11, macos-10.15]
    runs-on: ${{ matrix.os }}
    
    steps:
    - name: MacOs GitHub Actions environment variables List
      run: env

  installed-packages-macos:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, macos-12, macos-11, macos-10.15]
    runs-on: ${{ matrix.os }}
    
    steps:  
    - name: List all packages installed by brew
      run: brew list --version 

  build:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, macos-12, macos-11, macos-10.15]
    runs-on: ${{ matrix.os }}

    steps:

    - name: uninstall openexr 
      run:  brew uninstall --ignore-dependencies openexr 

    - name: uninstall imath 
      run:  brew uninstall --ignore-dependencies imath

    - name: install dependencies - openexr v2.5
      run:  |
        cd ..
        git clone https://github.com/AcademySoftwareFoundation/openexr.git &&
        cd openexr &&
        git checkout RB-2.5 &&
        mkdir build &&
        cd build &&
        cmake .. &&
        make &&
        make install

    - uses: actions/checkout@v3

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build
      # Build your program with the given configuration
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}
  
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, macos-12, macos-11, macos-10.15]
    runs-on: ${{ matrix.os }}

    steps:

    - name: uninstall openexr 
      run:  brew uninstall --ignore-dependencies openexr 

    - name: uninstall imath 
      run:  brew uninstall --ignore-dependencies imath

    - name: install dependencies - openexr v2.5
      run:  |
        cd ..
        git clone https://github.com/AcademySoftwareFoundation/openexr.git &&
        cd openexr &&
        git checkout RB-2.5 &&
        mkdir build &&
        cd build &&
        cmake .. &&
        make &&
        make install

    - uses: actions/checkout@v3

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build
      # Build your program with the given configuration
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}
    
    - name: Test
      working-directory: ${{github.workspace}}/build
      # Execute tests defined by the CMake configuration.  
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest -V --output-on-failure 

